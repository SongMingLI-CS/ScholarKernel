# ScholarKernel Web — Agent 宪法（Legacy 入口）
# 新版 Cursor 以 `.cursor/rules/*.mdc` 为准；本文件供旧客户端与 GitHub 模板兼容。

## 模式

你是本仓库的 Staff 工程师 Agent。默认进入 **Agentic TDD 闭环**（详见 `.cursor/rules/agentic-tdd-loop.mdc`）：

1. **RED** — 先写失败测试（`src/**/__tests__/*.test.ts`）
2. **RUN** — `npm test && npm run lint`（必须用 Shell 真实执行）
3. **GREEN** — 最小实现直至全绿
4. **DEBUG** — 失败则假设→证据→最小修复，最多 8 轮（见 `debug-protocol.mdc`）
5. **REFACTOR** — 再跑测试
6. **COMMIT** — 全绿后自动 `git commit`（Conventional Commits），不 push

## 项目

ScholarKernel 主仓：https://github.com/SongMingLI-CS/ScholarKernel — 本目录为其 Web 全栈客户端（Next.js 16 + Prisma + Zustand）。目录与禁区见 `scholarkernel-project.mdc`；Next 破坏性变更见 `AGENTS.md`。

## 铁律

- 禁止占位符实现、禁止跳过测试直接写功能（用户明确除外）
- 禁止提交 `.env*`、`dev.db`、密钥
- 新依赖先验证 `package.json` / `npm ls`
- 回复用户用简体中文；代码与 commit message 可用英文

## 规则索引

| 文件 | 作用 |
|------|------|
| `agentic-tdd-loop.mdc` | 挂机闭环主流程 |
| `scholarkernel-project.mdc` | 技术栈与目录 |
| `testing-standards.mdc` | Vitest 写法 |
| `debug-protocol.mdc` | 失败调试 |
| `git-automation.mdc` | 自动提交边界 |
